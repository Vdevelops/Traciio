{
  "name": "@repo/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "go run ./cmd/server/main.go",
    "dev:account": "cross-env DROP_TABLES=true ONLY_ACCOUNT=true go run ./cmd/server/main.go",
    "build": "go build -o bin/server ./cmd/server/main.go",
    "start": "./bin/server",
    "test": "go test ./...",
    "lint": "golangci-lint run",
    "clean": "go clean && rm -rf bin"
  },
  "engines": {
    "node": ">=18"
  }
}

